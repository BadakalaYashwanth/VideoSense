[build]
  # Using repo root as base context to avoid path resolution errors
  base = "" 
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
